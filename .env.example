# Copy to .env.local for local testing with `npx vercel dev`
# Add the same names in Vercel → Project → Settings → Environment Variables
MAILERLITE_API_KEY=your_mailerlite_api_token_here
MAILERLITE_GROUP_ID=188053062482397141
